#include "Bitmap.h"
#include "BitmapFileHeader.h"
#include "BitmapInfoHeader.h"
#include <iostream>

using namespace std;

/*
int main()
{
    fractalCreator::BitmapFileHeader fileHeader;
    fractalCreator::BitmapInfoHeader infoHeader;

    cout << "BitmapFileHeder-StructSizeMember: "
         << (sizeof(fileHeader.dataOffset) + sizeof(fileHeader.fileSize)
             + sizeof(fileHeader.reserved))
         << endl;
    cout << "BitmapFileHeder-StructSizeOverall: " << sizeof(fileHeader) << endl;

    //cout << "BitmapFileHeder-StructSizeMember: " << (sizeof(header.dataOffset) + sizeof(header.fielSize) + sizeof(header.reserved)) << endl;
    cout << "BitmapFileHeder-StructSizeOverall: " << sizeof(infoHeader) << endl;

    fractalCreator::Bitmap bmp(20, 20);
    //cout << "Bildsize: " << sizeof(bmp.m_pPixels) <<  endl;

	//bmp.showImagedata();

    char bla;
    cin >> bla;
    return 0;
}
*/
