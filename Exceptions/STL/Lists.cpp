#include <iostream>
#include <list>
using namespace std;

/*
int main()
{
    list<int> numbers;

    numbers.push_back(1);
    numbers.push_back(2);
    numbers.push_back(3);
    numbers.push_front(34);

    list<int>::iterator it1 = numbers.begin();
    //cout << "first Element " << *it1 << endl;
	it1++;
	numbers.insert(it1, 111);
	//it1++;
	cout << "Element " << *it1 << endl;

	list<int>::iterator eraseIt = numbers.begin();
	eraseIt++;
	cout << "EraseElement " << *eraseIt << endl;
	eraseIt = numbers.erase(eraseIt);
	cout << "EraseElement " << *eraseIt << endl;

    for (list<int>::iterator it = numbers.begin(); it != numbers.end(); it++)
    {
        cout << *it << endl;
    }

    char maeh;
    cin >> maeh;
    return 0;
}
*/