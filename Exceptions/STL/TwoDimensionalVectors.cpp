#include <iostream>
#include <vector>

using namespace std;

/*
int main()
{

	// 2dims vectors are vectors of vectors
	// define and initialize the vectors through a constructor-call:
	vector< vector<int> > grid(3, vector<int>(4,1));

	for (int row=0; row<grid.size(); row++)
	{
		for (int col=0;col<grid[row].size();col++)
		{
			cout << grid[row][col];
		}
		cout << endl;
	}

	char baeh;
	cin >> baeh;

	return 0;
}

*/