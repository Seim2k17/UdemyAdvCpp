#include <iostream>
#include <vector>
using namespace std;


/*
int main()
{
	vector<double> numbers(20);

	cout << "VectorSize: " << sizeof(numbers) << ", VectorCount " << numbers.size() << " Capacity: " << numbers.capacity() << endl;

	for (int i = 0; i < 100000; i++)
	{
		numbers.push_back(i);
		cout << "VectorSize: " << sizeof(numbers) << "Bytes, VectorCount " << numbers.size() << " Capacity: " << numbers.capacity() << endl;
	}

	

	char bla;
	cin >> bla;

	return 0;
}
*/